# 2026-08-05

## 부트스트랩

- v5 표준(티어: light)으로 프로젝트 생성.

<!-- 이후 세션은 아래에 append. 형식: ## 세션 N — 요약 / 한 일 / 시도했다 버린 것 / 다음에 볼 것 -->

## 세션 1 — 기획 승인 → 스캐폴딩 → 부트스트랩

**한 일**
- game-blueprint 로 정본 기획서 작성·승인 (`NHN/기획/FIVE_ALIBIS_기획서_v1.md`)
- Task 1: vite+ts+vitest 스캐폴딩, GitHub Pages 워크플로, Workers 프록시 스텁. 테스트 2/2, 빌드 OK
- GitHub private 레포 생성·푸시
- v5 부트스트랩 **light** — 티어 근거는 `decisions/001-티어선택.md`.
  요약: spec 계층을 기획서가 이미 수행했으므로 `docs/spec/` 을 만들면 정본이 둘이 된다

**시도했다 버린 것**
- `init.sh` 직접 실행 → `[ -e "$DEST" ]` 가드에 막힘. 임시 디렉토리 생성 후 비충돌 복사로 우회
- `@types/node` 없이 빌드 → `tsc` 가 `node:fs` 와 vite 타입을 못 읽어 실패. 추가 + `skipLibCheck`

**다음에 볼 것**
- Q1 배포 경로 (private repo + Pages 문제) — Cloudflare Pages 가 유력
- Task 2~5, 특히 **Task 5 배치 검증이 최대 마일스톤**

## 세션 1 (이어서) — 배포 경로 전환

**한 일**
- Q1 해결: **Cloudflare Pages 단일 오리진**으로 확정 (ADR 002)
- `workers/` 삭제 → `functions/api/{health,interrogate}.ts`. **CORS 코드 전부 제거**
- `.github/workflows/deploy.yml`(GH Pages) → `ci.yml`(테스트+타입체크+빌드)로 교체 — CI에 시크릿 불필요해짐
- 스모크 테스트 2→4개로 강화: CORS 헤더 부재 회귀 고정, 키 없을 때 폴백 신호, 전 소스 키 리터럴 스캔

**시도했다 버린 것**
- GitHub Pages 유지 → private repo는 무료 플랜에서 Pages 불가. public 전환은 제출 전 소스 공개라 기각
- Pages + 별도 Workers 2원 구성 → 오리진이 갈려 CORS가 남는다. Pages Functions면 한 배포로 끝

**다음에 볼 것**
- `wrangler login` 최초 1회는 **사람이 해야 한다** (브라우저 로그인) — 그 전엔 실배포 URL 없음
- Task 2~5, **Task 5 배치 검증이 최대 마일스톤**

## 세션 1 (이어서) — 실배포 성공

**한 일**
- Cloudflare Pages 실배포: https://nan-alibi.pages.dev
- 종단 검증 4종 통과 (게임 200 / health / interrogate 폴백 503 / CORS 부재)

**시도했다 버린 것 — 오진 1건**
- 유저가 "application authorisation fail" 을 보고해 **인증 실패로 오진**했다.
  `wrangler whoami` 로 확인하니 토큰은 멀쩡했고, 진짜 원인은 Pages 프로젝트 미생성이었다.
  → **에러 메시지가 가리키는 계층을 믿지 말고 실제 권한이 필요한 명령으로 검증할 것.**

**다음에 볼 것**
- Task 2~5, **Task 5 배치 검증이 최대 마일스톤**

## 세션 1 (이어서) — Task 2~5 완주, 최대 마일스톤 통과

**한 일**
- Task 2 시드 고정 RNG(mulberry32) + 코어 타입. `Math.random()` 직접 호출 금지를 테스트로 고정
- Task 3 결정론 사건 생성기 — 진실 궤적 / 진술(거짓말) / 물증 / 해금 사슬
- Task 4 검증기 V1~V7 + **BFS 해결탐색기** (상태=획득 항목 집합, 행동 3종, 무료 연결은 비용 0)
- Task 5 ★ 100시드 배치: 통과율 100%, 범인 S1~S5 고루 등장, 100시드 8ms

**발견하고 고친 것**
- **m\* 가 100건 전부 3으로 고정**이었다. 기준(3~5)은 통과하지만 사건 뼈대가 매번 같다는 뜻 —
  반복 플레이 가치가 이 게임의 근거인데 그게 죽는다. 사슬 깊이 3종 가변화로 3/4 분포 확보 (ADR 003)
- m\*=5 는 **일부러 안 만들었다**. 예산 6에서 여유 1회면 초회 플레이어가 사실상 진다
- 자체 가드가 자기 주석을 잡는 오탐 1건 — 주석 제거 후 검사하도록 테스트 수정

**다음에 볼 것**
- Task 6~7(리듀서·채점) → Task 8(프롬프트·검증기, LLM 모킹)
- 실플레이 테스트 때 초회 승률 보고, 쉬우면 m\* 말고 잡음·거짓말쟁이 수를 올릴 것

## 세션 1 (이어서) — Task 6~9, 백엔드 완성

**한 일**
- Task 6~7 상태 리듀서(불변)·모순 판정·채점. 조사 6회 예산, 연결은 무료
- Task 8 페르소나 카드 8종(목소리 4요소) + 프롬프트 조립 + 3중 검증기 + 폴백 2층
- Task 9 OpenAI 실연동. `functions/` 가 `src/engine` 을 직접 import — 사건을 **시드로 서버 재생성**해
  진실이 네트워크·브라우저에 안 실리게 했다

**실측으로 뒤집힌 것 2건**
- reasoning 토큰이 지연의 절반이었다 → `effort=none` 으로 3.1초 → 1.74초 (ADR 005)
- **캐시가 안 걸린다** — 입력 666토큰 < 최소 1,024. 프리픽스를 패딩해 억지로 켜는 건 본말전도라
  포기하고 기록했다. 판당 $0.02 라 무의미한 최적화였다

**철회한 계획 1건**
- `dialogue` 스킬의 `voice-check.ts` 로 C6 을 측정하려 했으나 **영어 전용이라 폐기**.
  한국어에서 contraction/fragment/question 비율이 무의미하고 `22:30` 을 단어로 센다.
  대신 `scripts/persona-audit.mjs` 를 직접 만들었다 (버릇 검출 + 문자 2-gram 유사도).
  첫 실행 8/8, 최대 유사도 21.4% — 그 쌍이 PERSONA_CONFLICTS 와 일치

**함정 1건**
- `functions/tsconfig.json` 에 `../src` 전체를 넣었더니 DOM 쓰는 `main.ts` 까지 검사돼 실패.
  엔진 계층만 포함하도록 좁혔고, 이제 이 설정이 **"엔진은 DOM 비의존" 을 강제하는 가드**가 됐다

**다음에 볼 것**
- Task 10~14 UI. 현재 배포본은 여전히 "scaffold ok"

## 세션 1 (이어서) — Task 10~14, 화면에서 한 판이 돈다

**한 일**
- 사건 생성기에 표면 데이터 추가 (이름·직업·피해자와의 관계·페르소나 배정, 충돌 조합 회피)
- 3열 레이아웃 / 용의자 카드 / 인터뷰 화면(타이핑 연출) / 증거 템플릿 4종 / 보드 / 제출·재구성
- 브라우저에서 종단 실측: 심문·조회·연결·모순 검출 전부 확인, 콘솔 0건

**브라우저에서만 드러난 버그 3건 (테스트로는 안 잡혔다)**
1. **기록 조회 버튼이 전부 "영수증 조회" 로 같아** 무엇을 여는지 모른 채 찍어야 했다.
   "선택의 우선순위가 전략" 이라는 설계가 통째로 죽는다 → 라벨에 시각·장소를 노출 (누가 찍혔는지는 계속 감춤)
2. **용의자 카드가 `div+onclick`** 이라 키보드로 게임의 첫 행동조차 불가능했다 → 버튼 시맨틱 + Enter/Space
3. **한글 IME 조합 중 Enter** — 조합 확정용 Enter 와 전송용 Enter 를 구분 안 해
   "안녕하세" 상태로 질문이 잘려 전송되고 **조사 1회가 되돌릴 수 없이 날아간다** → `isComposing` 방어

**도구 함정 1건**
- 브라우저 도구의 `key: Return` 이 입력에 안 닿아 "Enter 가 안 먹는다" 로 오진할 뻔했다.
  JS 로 같은 이벤트를 직접 디스패치해 **내 코드가 아니라 도구 문제**임을 갈랐다

**다음에 볼 것**
- Task 15~16 → `self-check` 전수 검수 → `game-texture`
