
## 새벽 — 사용자 실플레이 피드백 4건 처리

1. **심문 폴백** — vite dev 에 Pages Functions 가 없어 /api 가 404 였다.
   ssrLoadModule 브리지로 프로덕션 코드 그대로 로컬에서 돈다. (6965b1b)
2. **팔 관통** — pose-seated.py 결함 4곳 고치고 7종 재구움. manager 는 fix-arms 개선판.
   팔꿈치 43~53°, 관통 0, 팔꿈치가 몸 밖 19~23cm. (be098cb)
3. **의자** — "좌표 뽑아서 배치하라" 그대로. probe-chairs.mjs 로 실측(경찰서 23개,
   취조실 좌면 0.538m), 두 씬 다 실측 좌표에 앉힌다. (be098cb + 9d06179 에 씬 파일)
4. **인트로가 만화가 아니다** — comicPage.ts 로 다시 만듦. 종이 위에 칸이 쌓이는
   문법, 서술 상자·의성어·망점. ?introhold=1 로 영상 촬영용 수동 진행. (9d06179)

미해결: 로컬 TTS 는 .dev.vars 에 SUPERTONE_API_KEY 가 없어 폴백(브라우저 합성).
키는 사용자만 넣을 수 있다. 배포(Cloudflare)에는 있다.

## 발단 카툰 다중 페이지 검증 + 호텔 인트로 3페이지 전환
- GC001(`?case=gc001`) 3페이지×10칸 전 구간 브라우저 검증: 페이지 넘김(page-out/in), fx, 서술 상자, 진행점 10개, 자동 재생 완주 후 본게임 진입까지 확인.
- 에이전트의 새 시작 페이지(사건 시작 버튼) → 발단 카툰 연결이 이미 HMR로 작동 — playIntro 호출 지점이 유지된 덕.
- 호텔(시드) 인트로를 단일 5칸 페이지 → **3페이지(2+스플래시+2)**로 전환. "한 장에 다 몰아넣었다" 지적의 잔여 과제. playIntro 의 구 재생기 제거, playComicBook 으로 단일화.
- 함정: `.cpage`는 CSS 2열 그리드 — 행 정의가 한 토큰이면(`'p2'`) 칸이 왼쪽 열만 차지한다. 전폭 칸은 `'p2 p2'`로 스팬해야 한다.
- 함정: 앱이 사건 로드 시 URL을 `?seed=N`으로 replaceState — `introhold`/`case` 쿼리가 리로드 후 사라진다. HOLD_MANUAL 은 모듈 로드 시점 평가라 첫 진입은 안전.

## ADR 022 구현 — 챕터 예산·3축 추론·엔딩 (에이전트, 커밋 3개)

- **S1 엔진** (1c435ad): FIELD_BUDGET=5 / TALK_CAP=10 지갑 분리, WEAPONS 5종+검시 소견
  ('autopsy' 물증, 셔플 뒤 append — 주 스트림 보존), 용의자 나이·사정(범인 것이 동기),
  submit 3축(60/15/15) + 통찰 +10 + 잔여 현장 ×2. 봇 재작성, 밸런스 재측정
  (random 22 / common 84 / optimal 100 — 구간 75~95 로 재설정, ADR 023 §3).
- **S2 흐름** (88e8ca6): 시작 페이지(자동 시작 제거, 클릭이 wake 겸용) → 챕터 게이트
  (심문 진입 잠금 + 배너) → 예산 소진 순간 수사 정리 1회 → 심문 화면(나이·관계·대화 n/10 칩).
- **S3 엔딩** (1ec3ff0): 페르소나 8종 결정론 자백(confessions.ts, LLM 아님) ·
  5단계 재구성(발단→전개→위기→절정→결말, 결과 시트 텍스트 타임라인) · 3축 O·X ·
  오답 진단에 부족한 축 안내(비스포일러) · [같은 사건 다시]/[새 게임].
- 브라우저 실확인: seed 36 정답 경로(자백·5단계·100점)와 오답 경로(미제 편철·축 안내),
  게이트 차단 배너, 수사 정리 자동 표시, retry 재로드까지.
- 함정 (ADR 023 에 정리): ① 즉시 조회 기록 4건뿐인 시드 — 예산 0 만으로는 게이트가
  영영 안 열린다 → fieldDone 은 "기록 고갈" OR ② 예산 소진 후 해금 기록은 무료 조회
  (아니면 통찰 보너스가 구조적으로 불가능) ③ 무작위 봇이 지목 사다리를 타면 51% 를
  "추리" — 하한선 의미 복구를 위해 지목도 무작위로.
- 남긴 것: 시작 페이지·게이트·수사 정리는 headless 테스트 없음(main.ts 는 게이트 밖) —
  브라우저 확인으로 갈음. 밸런스 구간 75~95 는 팀 검토 대상 (QUESTIONS Q4).

## GC-001 어댑테이션 — 갤러리 사건이 챕터 엔진 위에서 돈다 (에이전트, 커밋 2개)
- **C1 월드 라벨 간접화** (8ef3c0e): CaseFile.world + slotLabel/placeLabel/kindLabel 헬퍼,
  10파일 치환(프롬프트·검증기 포함 — 호텔 어휘 누출 금지). "결정적=범행 시각·현장" 가정
  제거(플래그 못박기). 게이트 통과: 시드 풀 400개 결정론·기존 228건 무변화.
- **C2 gc001 고정 사건** (923bd0d): src/data/gc001.ts 수제 CaseFile(기록 9건, 결정적은
  은폐 시각 21:18, 열쇠는 기록 2건), ?case=gc001 진입+Retry 유지, ending 오버라이드
  (자백·5단계 결정론), tests/gc001.test.ts 12건(후보 전이 5→3→1, §15.8 붕괴, 금칙어 스캔).
- 실플레이에서 잡은 버그: **예산 0 이면 해금 기록 버튼까지 비활성** — 엔진의 무료 조회가
  화면에서만 죽어 통찰 보너스 도달 불가(호텔도 동일). 헤드리스 테스트는 엔진만 봐서
  못 잡았다 — 버튼 상태는 브라우저만 안다.
- 브라우저 완주: 발단 카툰→현장 5회→수사 정리(후보 3명)→김하늘 제시(실 LLM)→
  무료 해금 2건→후보 1명→3축 100점→gc001 자백·5단계→Retry ?case 유지.
- 계약과 달라진 세부 4건은 ADR 024 에 (ending 필드·Evidence.note·정황 확인 라벨·V3 완화).

## GC-001 본게임 통합 검증 (메인 세션)
- `?case=gc001` 실플레이: 조사 5회(E1·E2·E4·E5·E7) → 후보 {류나린,배지호,문소라} → 류나린 정면 심문
  실 LLM "그게 기록에 있습니까? 전 그런 일을 한 적이 없습니다"(계산적 성향·부인·어휘 누출 0) →
  3축 정답 제출 → 검거 카툰(류나린 이름·직책 정확) → 결과 시트 "사건 완전 해결"+정직한 진단
  ("좁혀서 맞힌 게 아니라 골랐다") + 전용 자백 + 5단계.
- 발견: E3/E6 을 안 열고 예산을 쓰면 결정적 사슬이 그 판에서 닫힌다 — 의도된 전략 선택이고
  결과 시트가 이를 정직하게 짚는다. 버그 아님.
- outro 사건 종속화: 마지막 칸 그림·문장이 사건을 따른다 (gc001=갤러리 외관).

## 월드 확장 + 몰입 루프 (에이전트, 커밋 3개)
- **P0** (3c11ebc): gc001 3D 복구 — 갤러리 직업 5종 + 월드 팩 직업 15종을 SLUG_BY_JOB 에
  매핑(모델 외형은 gen-characters 프롬프트 기준). "매핑 누락은 테스트가 먼저 빨간불" 잠금.
- **P1** (25e02fc): 월드 팩 3종(단원 경매장·한내 방송사·백야 극장) — applyWorld 스킨,
  ?world= 진입, 새 게임 로테이션, Retry world+seed 유지. **서버에 world·caseId 전달** —
  시드만 보내면 프롬프트가 호텔 어휘였다(gc001 잠복 버그 포함 수정). ADR 025.
- **P2** (79a30ab): 타이핑 인디케이터(점 3개+단계별 독백), 응답 도착 paper 음,
  브리핑/수사정리 페이드아웃, 게이트·코치 디에게틱 문구, 경찰서 3D 로딩 한 줄.
- 브라우저 확인: gc001 취조실 3D(secretary 모델)·경찰서 탭 복구, ?world=auction&seed=36
  (봉인된 출품작·경매홀·직업 5종), ?seed=36 호텔 불변, 로딩 표시 소거, 타이핑 표시.
- 테스트 249건 초록. 시드 사건 결정론 게이트(같은 참조 반환을 === 로 잠금) 통과.

## 몰입 루프 2패스 — 판정 인장·타자기·자물쇠 소리 (에이전트, 76f771e)
- 결과 시트 안무: 인장 내리찍기(verdict 음+흔들림) → 3축 O·X 순차 → 카운트업 → 줄 페이드.
  타이밍 상수 RESULT_FX(main.ts). rAF 함정: 안 그려지는 탭에서 카운트업이 0에 얼어붙어
  마감 시각 최종값 못박기 추가.
- 타자기: 3글자당 type 음(45ms 스로틀)·커서 ▌·완료 시 typebell. **선재 버그 발견** —
  onStage 재렌더가 타이핑 줄을 매번 완성문으로 갈아치우고 있었다(음성 단계 전환마다).
  타이핑 중 스테이지 재렌더 유예로 해결. TYPE_FX 상수.
- 제시로 자물쇠가 풀리면 open 대신 unlock — 증언 개방과 결정적 사슬을 소리로 구별.
- reduced-motion 전부 즉시 표시 + CSS 이중 잠금. 249 테스트 초록.

## 배포 직후 버그 2건 — 공통 원인은 경찰서 이중 배치 경쟁 (에이전트)
- 신고: "E 를 눌러도 심문이 안 뜬다" · "다리가 안 떨어져 보인다".
- 수치로 지운 가설: 좌석 도달성(probe-seats — 5좌석 전부 1.1m 안 도달 칸 3~9개),
  리그 위상(probe-legs — 걷기 7종 상관 -0.96~-0.98 교차 보행, 착석 8종 무릎 86~93°·간격 정상).
- 진범(스크린샷 실측): setSeats 비동기 재진입 → **같은 사람이 두 의자에 두 번** —
  E 는 마지막 런의 등록 좌표만 보고(무반응), 겹친 모델의 다리가 얽힘(붙어 보임).
- 수리: seatEpoch 토큰으로 낡은 런 폐기 + 그린 사람만 근접 판정 + 게이트 잠금 중
  3D 힌트바가 연행 불가 사유를 말함. probe 2종을 npm 스크립트로 등록(회귀 잠금).

## 「30초의 현장」 개편 구현 (에이전트, 커밋 5개)
- **규칙 계층** (d630608): sceneRules.ts — 훑기 5s→수집 30.0s 상태기계, 압박 곡선
  (1Hz→2Hz→심박), timeUp(예산 0 강제), swapField(환불+재조회 — 지출은 엔진만),
  12×9m 방 배치·결정론 스폰. 테스트 20건이 기획서 ⑥ 헤드리스 몫을 잠금.
- **씬+카드 월** (ffde0e2): crimescene3d.ts(프리미티브 방·오빗 훑기·마커 실루엣·
  가방 5칸 HUD·비네트·화이트아웃·스왑 패널·?calm=1) + cardwall.ts(코르크 보드
  5장·초상·뒤집기·소거 대각선+인장) + main 배선(브리핑→씬→정리→월→심문).
  3D 실패 시 기존 기록철 흐름 폴백. 테스트 274건.
- **씬 시계의 함정**: 벽시계는 탭이 멎은 동안 30초가 몰래 탄다(실측) →
  프레임 간격 누적 + 1초 초과 간격 무시로 확정 (ADR 026 §4).
- **빌보드 아이콘 배선** (263d904): public/evidence/<kind>.webp 있으면 스프라이트,
  없으면 프리미티브 — 에셋 0 원칙. 자물쇠 스프라이트 배율이 펄스에 덮이던 것도 수리.
- **60s 문법 5종** (5dbd60a): 놋쇠 회중시계(붉은 부채꼴+침 떨림)·근접 흰 윤곽빛·
  수거 포물선 비행(착지에 snap)·소품 1.6배·컷어웨이(카메라 쪽 벽 낮춤).
- **달리기 배선** (9bcbf1c): run 클립 우선+3.4m/s, 리그 키 검증(1.2~2.5m) 실패 시
  걷기 폴백 — security.run 이 키 31.26m 로 걸렸다(재익스포트 대기).
- 브라우저 확인: seed36 수거→5/5→스왑(숫자키)→철수→정리→카드 월→심문,
  gc001 시간 종료 경로, ?world=auction, ?calm=1(타이머 없음), 콘솔 에러 0.

## 애니메이션 2차 배선 — run 수리 확인·pickup 원샷 (에이전트, 3348233)
- run 재수리본(strip-beta) 7종이 리그 가드(1.2~2.5m)를 통과 — 달리기 3.4m/s 활성 실확인.
- pickup: 같은 리그의 다른 GLB에서 클립만 뽑아 액터 mixer 에 본 이름으로 바인딩.
  unrollLegs 의 rest 는 그 파일 자신의 씬(액터는 이미 rest 가 아니다). 이동 잠금 0.45s
  안에 앞 55%를 timeScale 로 접어 재생, 걷기 weight 0. E 수거에서 숙임→복귀 실확인.
- turn180 미배선 — 12rad/s 조향 보간과 충돌(클릭마다 급반전 원샷 오발). ADR 026 §8.

## 실플레이 3건 수리 + 실모델 배선 (에이전트, d7d349f·4e6ca96)
- **진짜 갤러리**: art_gallery.glb 23MB→1.8MB(2048·webp·draco·join해제). 서관 홀 실측
  오프셋(+1.5,+0.6), 벽격자 베이크(모서리 래스터화). 함정: floor 단차·corner 픽처레일이
  홀 전폭 유령 벽 — 도달성 BFS(마커 10/10)로 잡고 /floor|stairs|corner/ 베이크 제외.
- **순간이동 수리**: 이동·클립이 dt 캡만 타던 것 → 실시간 simSec + 0.05s 충돌 서브스텝.
- **A포즈 수리**: Breathing Idle 7종 리타게팅(서브에이전트, run 파이프라인) → 대기 기본
  + 집기/이동 weight 블렌딩. run 재수리본 가드 통과(달리기 3.4). turn180 기각(ADR 027 §5).
- **증거품 실모델**: ev-cctv/call/receipt/autopsy 마커 교체(실모델>빌보드>프리미티브),
  재질 인스턴스 복제로 흰 윤곽빛 격리, crate 스킨. keycard 는 프리미티브 유지.
- CREDITS: 갤러리+소품 7종 "출처 확인 필요" — 제출 전 확인/교체 필수. 테스트 274 유지.
