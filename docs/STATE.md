# STATE — nan-alibi (FIVE ALIBIS)

> 덮어쓰는 세이브 파일. 항상 "지금" 기준. 히스토리는 [journal/](journal/), 결정 근거는 [decisions/](decisions/).
> 마지막 갱신: 2026-08-10

## 목표 (이게 되면 끝났다고 말할 수 있는 조건)

**매 판 다른 사건이 생성되고, 5명의 AI 페르소나를 심문해 3~5분 안에 범인을 지목하는
브라우저 추리게임을, 2026-08-10 NAN 2026 사전과제로 제출한다.** (마감 8/7 → 8/10 연장)

예산 체계가 두 번 바뀌었다: 6회 → 9회(ADR 009) → **현장 조사 5회 + 인당 대화 10회 상한**(ADR 022, 2026-08-10).
아래 완료 기준 B 의 "6회" 와 본문 곳곳의 "9회" 는 그 이전 기준이다. 추론도 3축(범인·동기·도구)이 됐다.

끝났다고 말할 수 있는 조건 = 기획서 §6 완료 기준 A~E 전부 통과:
- A. 사건 로직 — 시드 100개 검증 통과율 ≥95%, **최소 조사 수 m\* 가 3~5** (최대 마일스톤)
- B. 게임 규칙 — 조사 6회 예산, 연결 무료, 모순 판정, 채점
- C. AI 계층 — 화이트리스트 검증·폴백 3층·캐시 히트
- D. 성능 — 첫 토큰 ≤1.2s, p95 ≤3.5s, 1판 ≤$0.15, 콘솔 0건
- E. 시간 — 숙련자 1판 ≤5분

**정본 기획서:** `~/Project/NHN/기획/FIVE_ALIBIS_기획서_v1.md` (승인됨 2026-08-05)

## 현재 상태

- 2026-08-05 v5 부트스트랩 (티어: light) — 코드 스캐폴딩 위에 문서 구조만 얹음
- 2026-08-05 **Task 1 완료** — vite+ts+vitest, GitHub Pages 워크플로, Workers 프록시 스텁.
  테스트 2/2, 빌드 성공, `dist` 경로 `./assets/...` 확인. 커밋 `815adee`
- 2026-08-05 GitHub 레포 생성·푸시 (private): https://github.com/teobasaki/nan-alibi
- 2026-08-05 **배포 경로 확정 → Cloudflare Pages 단일 오리진** (ADR 002). `workers/` 제거,
  `functions/api/` 로 전환. CORS 코드 소멸. 테스트 4/4, functions 타입체크·빌드 통과
- 2026-08-05 **LLM 공급자 OpenAI 확정** (ADR 004). 2026-08-09 모델을 **`gpt-4o`** 로 내렸다 (ADR 020) — 검증 통과율 같고 더 싸고 더 빨랐다.
  `OPENAI_API_KEY` Pages Secret 등록 완료. 캐시 수동 제어는 유지(`prompt_cache_breakpoint`), 프리워밍만 손실
- 2026-08-05 **Task 2~5 완료 ★ 최대 마일스톤 통과.** 테스트 43/43
  - 시드 고정 RNG(mulberry32) + 코어 타입 + 결정론 사건 생성기 + 검증기 V1~V7 + BFS 해결탐색기
  - **100시드 배치: 원생성 통과율 100%, m\* 분포 3회58/4회42, 범인 S1~S5 전부, 8ms**
  - m\* 고정(전부 3) 문제를 사슬 깊이 가변화로 해결 (ADR 003) + 분산 회귀 감시 추가
- 2026-08-05 **Task 10~14 완료. 화면에서 한 판이 돈다.** 테스트 90/90
  - 3열 레이아웃 · 용의자 카드 · 인터뷰(타이핑 연출) · 증거 템플릿 4종 · 보드 연결 · 제출/재구성
  - **브라우저 실측:** 심문 6→5 · 조회 5→4 · 연결 9회에도 예산 불변(무료) ·
    모순 실제 검출("권도윤의 22:10 진술이 E1 기록과 어긋난다") · **콘솔 에러 0건**
  - 실측 중 잡은 버그 3건: 조회 라벨이 전부 동일해 찍기밖에 못 함 / 용의자 카드가 키보드 접근 불가 /
    **한글 IME 조합 중 Enter 로 질문이 잘린 채 전송** (조사 1회가 그대로 날아가는 버그)
- 2026-08-05 **Task 6~9 완료. 백엔드 전 계층 완성.** 테스트 86/86
  - Task 6~7 상태 리듀서·모순 판정·채점 (B1~B5)
  - Task 8 페르소나 8종 + 프롬프트 조립 + 구조화 스키마 + **3중 검증기** (C1~C3)
  - Task 9 OpenAI 실연동 — `gpt-5.6-terra` + `reasoning.effort=none` (ADR 005)
  - **프로덕션 실측 12회: 1.35~2.71초, 재시도 0, 폴백 0**
  - **페르소나 감사 8/8 통과, 최대 유사도 21.4%** (`npm run audit:persona`)
- 2026-08-05 **실배포 성공 → https://nan-alibi.pages.dev** (Account `1524bb03...`).
  검증: 게임 200 / `/api/health` `{ok:true,hasKey:false}` / `/api/interrogate` `503 {fallback:true}` /
  CORS 헤더 없음. 배포 파이프라인 종단 확인 완료

- 2026-08-06~07 **3D 심문실 도입** (ADR 016). Sketchfab 취조실 + 캐릭터 8종.
  Meshy 로 뽑은 5종 + 기성 리깅 모델 3종(carla/alina/wong) 혼용 — 기성 모델은 89~104 본에 얼굴 리그가 있어 립싱크 여지가 있다
  - 블렌더 실측으로 좌표계 통일 · `export_rest_position_armature=False` 로 착석 포즈 유실 해결
  - **Meshy 크레딧 690 중 305(44%) 낭비.** 원인 둘 다 습관 문제였다 — ① 1명으로 먼저 검증하지 않고 8명을 한꺼번에 태움
    ② 도구 출력을 확인하지 않고 다음 단계에 먹임(`sips --cropOffset` 이 4칸 중 1칸에서 조용히 실패). 둘 다 스크립트에 검증을 박아 막았다
- 2026-08-07 **인트로·엔딩 시네마틱** — 웹툰식 자막 패널. 그림이 없으면 색면+큰 활자로 폴백
- 2026-08-08 **QA 피드백 1차 반영** (`FIVE_ALIBIS_user_journey_painpoints_analysis.md`)
  - 5.2 질문의 목적 표시 · 5.3 대사와 조서 분리 · 5.6 판정 4등급 · 5.7 오답 시 진범 비공개(진단만) ·
    5.8 결말을 이야기 4비트로 · 5.9 페르소나를 전략 팁에서 관찰 가능한 행동으로 재작성
  - 이때 **옆문 누출**을 잡았다 — 진단 문구는 조심스럽게 썼는데 사건 재구성표·수단 해설이 정답을 그대로 흘리고 있었다
- 2026-08-08 **QA 5.4 — 상황판을 세 각도로** (ADR 017). 테스트 131건. → https://db620018.nan-alibi.pages.dev
  - 인물 × 시각(기존) + **장소 × 시각(신규)** + **인물별 조서(신규)**. 미대조 조합 장부 추가
  - 축을 뒤집으니 인물 격자가 못 보여주던 게 나온다 — 한 칸에 이름이 모이면 서로의 알리바이,
    한 이름이 같은 세로줄에 두 번이면 둘 중 하나는 거짓. **판정은 끝까지 시스템이 하지 않는다**

## 다음 할 일

- [ ] **QA 5.1** 조사 횟수 — 이미 9회로 올렸다(보고서는 6회 기준). 조서가 붙어 한 번의 값이 커졌으니 **실제로 부족한지 다시 재야** 한다
- [ ] **QA 5.5** "로그라이트" 표기 — 문서에서 그 단어를 못 찾았다. 어디를 가리키는지 확인 필요
- [ ] Sketchfab 에셋 **라이선스 확인** (취조실 + 캐릭터 3종) — 제출물에 들어가므로 마감 전 필수
- [ ] E1(플레이 시간) 사람 실측 — 여전히 미측정
- [ ] `self-check` 스킬로 완료기준 A~E 전수 검수

## 안 되는 것 / 막힌 것 (정직하게 — "다 잘 됨"이라고 쓰지 마라)

- **프롬프트 캐시가 전혀 안 걸린다.** 입력이 666토큰이라 OpenAI 최소치 1,024를 못 넘긴다.
  비용($0.02/판)·지연(1.7초) 모두 예산 안이라 **의도적으로 방치**한다 (ADR 005). 프리픽스를 억지로 늘리지 않는다
- **E1(플레이 시간) 미측정.** 사람이 한 판을 처음부터 끝까지 돌려봐야 한다
- **캐릭터 품질이 고르지 않다.** 기성 리깅 모델 3종과 Meshy 5종이 섞여 있어 화풍이 안 맞는다.
  Meshy 로는 더 못 올린다 — 이미지 4장 상한 때문에 "선명한 얼굴"과 "온전한 전신"이 동시에 성립하지 않는다
- **Sketchfab 에셋 라이선스 미확인** (취조실 + 캐릭터 3종). 해커톤 제출물이므로 마감 전에 반드시 확인해야 한다
- **TTS 실제 가청 여부 미확인.** 코드는 Yuna 우선으로 붙어 있으나 소리는 사람이 들어야 확인된다
- ~~로컬 vite dev 로는 AI 경로가 검증되지 않는다~~ **해소** — ssrLoadModule 브리지로 로컬에서도
  `/api/interrogate`·`/api/tts` 가 돌고, 2026-08-10 실측 둘 다 200 (아래 갱신)
- 표정은 2종(평정/동요)만 있다. 3종째(붕괴)는 미구현
- D2(첫 토큰 ≤1.2초)는 스트리밍 없이 불가 → "응답 완료 ≤2.5초"로 재정의 (ADR 005)
- wrangler 4.x 가 신규 프로젝트에 **Pages 대신 Workers(static assets)** 를 권한다.
  Pages 는 정상 동작하므로 마감 전 전환하지 않는다 — 마감 후 재검토 (ADR 002 에 기록)

## 열린 질문

- [QUESTIONS.md](QUESTIONS.md) 참조

## 2026-08-10 새벽 갱신
- 로컬 dev 에서도 심문·TTS API 가 돈다 (vite ssrLoadModule 브리지)
- 착석 8종: 팔꿈치 43~53°·관통 0·재굽기 완료. 두 씬 모두 **실측 의자 좌표**에 앉는다
- 인트로·아웃트로 = 만화 페이지 (comicPage.ts). `?introhold=1` 촬영용 수동 진행
- **막힌 것**: 로컬 TTS 는 SUPERTONE_API_KEY 가 .dev.vars 에 없어 브라우저 합성 폴백

## 2026-08-10 낮 갱신 — ADR 022 팀 UX 계약 구현 완료
- **챕터 흐름이 돈다**: 시작 페이지 → 발단 카툰 → 현장 조사 5회 → 수사 정리 →
  심문(인당 대화 10회 상한) → 최종 추론 3축(범인 60·동기 15·도구 15) →
  정답(자백+5단계 재구성+O·X)/오답(비스포일러 진단+축 안내) → 같은 사건 다시/새 게임
- 커밋 3개: 1c435ad(엔진) · 88e8ca6(흐름) · 1ec3ff0(엔딩). 테스트 228건 전부 초록
- 검시 소견('autopsy') 물증 추가 — 도구 축의 근거. **시드 풀 400개 결정론 보존**
  (새 추첨은 전부 파생 rng 스트림, 검시 기록은 셔플 뒤 append)
- 자백은 LLM 아님 — 페르소나 8종 결정론 템플릿 (src/data/confessions.ts)
- 밸런스 재측정: random 22% / common 84% / optimal 100%. 목표 구간 75~95 로 재설정 —
  **팀 검토 필요** (QUESTIONS Q4, 근거는 ADR 023)
- **막힌 것 없음.** 브라우저 확인은 seed 36 정답/오답 경로만 했다 — 전 시드 훑기는 안 했다

## 2026-08-10 낮 갱신 2 — 발단 카툰 다중 페이지 + 통합 검증 (커밋 c5bfeeb)
- **골든 케이스 001 경로**: 시작 페이지 각주 버튼(.start-alt) → `?case=gc001` →
  「옮겨진 상자의 사각」 3페이지×10칸(평온→위화감→충격→호기심) → 본게임.
  단, **본게임 데이터는 아직 시드 사건** — 갤러리 CaseFile 연결은 미착수 (PLACE_LABEL 리팩터링 필요)
- 호텔(시드) 인트로도 3페이지(2+스플래시+2)로 전환. 아웃트로 검거 4칸·미제 3칸(m0~m2) 실화면 검증
- **로컬 실 LLM 심문 실측**: 조사 5회 소진 → 게이트 해제 → 후보 소거 5→3 → 배주원 심문 —
  응답에 성향 반영, `POST /api/interrogate` 200 + `POST /api/tts` 200(Supertone), 대화 칩 10→9
- CREDITS 정정: 만화 패널 22장 = ChatGPT 세션 생성, 음성 = Supertone + Web Speech 폴백
- 남은 확인: 대화 10회 전소진 페이싱, 경찰서 3D 연행 게이트·검시 마커, 스피커 가청 확인,
  제출물(영상·PDF), 배포

## 2026-08-10 오후 갱신 — GC-001 「옮겨진 상자의 사각」 플레이 가능
- `?case=gc001` 로 진입하는 고정 갤러리 사건이 챕터 엔진 위에서 완주된다 (커밋 8ef3c0e·923bd0d)
- 월드 라벨 간접화로 라벨·어휘를 사건이 소유한다 — 시드 사건 400개 결정론은 그대로 (게이트 통과)
- 결정적 기록이 은폐 시각(21:18)에 있는 첫 사건 — "결정적=범행 시각" 가정을 엔진에서 걷어냈다
- 수단 축은 추상 분류(도구·유혈 어휘 금칙, 테스트가 잠금). 자백·5단계는 사건 소유 결정론
- 테스트 240건 초록 (gc001 12건 포함). 세부 차이는 ADR 024

## 2026-08-10 저녁 갱신 — 월드 3종 + 몰입 루프
- `?world=auction|studio|theater` 로 시드 사건이 옷을 갈아입는다 (스킨, ADR 025).
  [새 게임] 이 무대를 로테이션. 서버 프롬프트도 같은 옷(world·caseId 전달)
- gc001 3D 복구(직업→모델 매핑) — 매핑 누락은 tests/worlds.test.ts 가 잠근다
- 몰입 루프: 심문 대기 타이핑 인디케이터·전환 페이드·디에게틱 안내·3D 로딩 표시
- 테스트 249건 초록. 확인 URL: ?seed=36(호텔 불변) · ?world=auction&seed=36 ·
  ?world=studio&seed=36 · ?world=theater&seed=36 · ?case=gc001

## 2026-08-10 밤 갱신 — 「30초의 현장」 개편 구현 완료 (승인 기획서)
- **새 흐름이 돈다**: 인트로 → 브리핑(문구 갱신) → **현장 씬**(훑기 5s 오빗 →
  30.0초 수집, 가방 5칸, 스왑, 압박 곡선, 회중시계) → 호루라기·화이트아웃 →
  수사 정리(기존 게이트) → **프로필 카드 월**(코르크 보드 5장·소거 인장) → 심문(기존).
  경찰서 3D·대조표는 카드 월의 보조 동선으로 유지.
- **엔진 무변경**: 수거 = lookupEvidence, 스왑 = 환불+재조회, 시간 종료 = 예산 0 강제.
  세칙·버린 대안은 ADR 026. 커밋 5개(d630608·ffde0e2·263d904·5dbd60a·9bcbf1c)
- 테스트 274건 초록 (기존 249 유지 + 현장 20 + 카드 월 5). 콘솔 에러 0
- 3D 실패·저사양이면 기존 기록철 흐름으로 폴백. `?calm=1` = 타이머 없음(심사 데모)
- 애니메이션 2차 배선(3348233): run 재수리본(strip-beta) 가드 통과 → **달리기 3.4m/s
  활성** · pickup 클립 원샷(0.45s 잠금·앞 55%·걷기 weight 0) — E 수거에 몸이 숙는다.
  turn180 은 조향 보간과 충돌해 **배선 안 함** (ADR 026 §8, 제출 후 재검토)
- 실플레이 3건 수리(d7d349f) + 실모델 배선(4e6ca96), 세칙 ADR 027:
  ① **진짜 갤러리 방** — 서관 홀 실측 통합, 벽격자 베이크(픽처레일 유령 벽은 BFS 로
  잡아 제외, 마커 도달 10/10), 조명 증폭 ② **순간이동 수리** — 실시간 simSec +
  0.05s 충돌 서브스텝 ③ **A포즈 수리** — Breathing Idle 7종, 집기/이동/대기 블렌딩
  ④ 증거품 실모델 4종 + crate 스킨 (표현 서열: 실모델>빌보드>프리미티브)
- ⚠ **갤러리·소품 7종 출처 미확인** (CREDITS "출처 확인 필요") — 제출 전 확인/교체 필수

## 2026-08-10 밤 갱신 2 — 현장 씬 개편 라운드 (커밋 a529ea9, ADR 028)
- **액터 = Joe 확정** (Mixamo 네이티브, unrollLegs 무적용). joe.run 이 키 가드에
  걸려(30.78m 잔여) run **클립만** 걷기 몸에 잇는 2단 폴백 — 실측 달리기 3.4m/s·
  대기·집기 전부 활성. 기존 슬러그 액터는 보정 유지 폴백
- **1인칭 기본**(EYE_HEIGHT 1.64) · V 탑다운 토글 · 훑기 5초 오빗 유지 ·
  수집 시작 순간 시점 안내 1회 · 클릭 이동은 몸이 목표를 향해 돈다
- **kind 서사 앵커 배치**(spawnAnchored): cctv 벽 상단 2.2~2.6m 부착 ·
  call 데스크/벤치 위 · receipt 데스크/수장고 곁 · autopsy 사망 지점 옆 ·
  keycard 수장고 구석(잿빛 틴트). 같은 kind 앵커 분산, 초과분 결정론 고리
- **사망 지점 = 조각상 아래**(갤러리 실측): fallscene 잿빛 틴트(§4 대비),
  crimetape Plane 리본 4변("DO NOT CROSS" 실확인), 링 테이프는 폴백으로 존치
- 브리핑 버튼 연타 재진입 가드(고아 캔버스 실측 재현 후 수리)
- 테스트 274 → **280** (kind 앵커 6건) · 콘솔 에러 0 · 엔진 무변경 ·
  실주행: calm 1판 — E 수거(바닥·벽 부착 모두)·가방 폴라로이드·철수→심문 게이트 확인
- 남은 확인(사람): 30초 실시간 페이싱 체감 · 스피커 가청 · 실기기 60fps ·
  1인칭 벽 cctv 시인성(ADR 028 알려진 흠)
- **실플레이 재보고 3건 처리** (커밋 cea783c, ADR 028 §9): ① "증거품 중복" —
  **중복 아님**(마커 1개 = 몸통 + 발치 링 + 봉인 자물쇠. 실측 몸통 10 · 링 10 ·
  자물쇠 1 · 중복 0 · 드로우콜 58). DEV 불변식과 헤드리스 유일성 2건으로 잠금
  ② "계속 달린다" — 클립 판정이 **입력 플래그**였다 → **실제 속도**가 정한다
  (실측 정지 후 speed 3.38→1.21→0.78, idleW 0.03→0.32→0.47). 훑기 5초 동안
  믹서가 안 돌아 대기가 0프레임에 얼던 것도 함께 수리 ③ "너무 빠르다" — 1인칭
  2.4 상한 · 조감 3.4 유지 · 가감속 램프 tau 0.15s · 클립 속도 동기화.
  **예산 불변**(지시), 3~5개 수집은 산수로 성립 — 실시간 완주는 사람 몫
- **표현 변주** (커밋 786316e, ADR 028 §10): 사용자가 본 "중복"의 진짜 원인은
  같은 kind = 같은 GLB 하나였다. cctv = 카메라 ↔ **필름 릴**, call = 전화기 ↔
  **출력 내역**으로 교대(실측 seed 36: 카메라 3 + 릴 2 / 전화기 1 + 서류 1) +
  증거 id 결정론 자세 변주(yaw·기울기·크기 ±12%) + 실모델 상시 회전 제거.
  **수거·기록 매핑 불변**, 테스트 288 → 296
- **실플레이 피드백 4건 처리** (커밋 4408360, ADR 028 §8): ① 충돌 — 재현 실패
  (실측: 파티션 슬라이드·벤치·조각상 전부 막힘), 역방향 검증을 DEV 에 상설
  ② joe.run 직진 재베이크 반영 — 브라우저 캐시가 범인이었다 (강제 갱신 후 가드
  통과, 달리기 몸 직행) ③ 집기 미재생 = idle 반반 블렌딩 — idle weight 즉시 0 +
  1인칭 카메라 숙임 0.38m ④ 수거 토스트·슬롯 툴팁·1~5키 확인·아이콘 자리
- **막힌 것 / 대기**: ① evidence/<kind>.webp 아이콘 미도착 — 오면 자동 빌보드
  ② tick·heartbeat·whistle·snap 은 파일이 있으나 pickup 키는 sound.ts 배선 대기
  ③ 실기기 60fps·스피커 가청은 사람 확인 필요 ④ 스왑의 내려놓기가 일지에 안 남아
  일지 줄 수와 잔량 눈금이 어긋나 보일 수 있음 (ADR 026 알려진 흠)

## 2026-08-10 밤 갱신 3 — 심문/수첩 시각 모드 분리 (ADR 030)
- 심문이 수첩 종이 테마를 상속하던 구조를 `.nb.interview` 상태 스코프로 분리.
  현재 심문은 어두운 3열(용의자 / 확대 인물+대화 / 변화 큐), 수첩은 크림 양면을 유지한다.
- 청록 선택·파랑 NEW·주황 CHANGED·적색 모순으로 색 의미를 복구했고, 대화는 무대
  하단 두 블록, 질문은 NEXT ACTION 바에 압축했다.
- 수첩 비교는 엔진에 없는 진술 번복을 만들지 않고 실제 `본인 진술 ↔ 객관 기록`
  모순을 양면에 표시한다.
- `npm run verify`: 22파일·308테스트, functions 타입검사, 프로덕션 빌드 통과.
  GC-001 로컬 심문 1920px·1024×768 확인, 콘솔 오류 0건(Three.js 기존 경고만).
- 남은 사람 확인: 모순을 실제로 만든 뒤 수첩 양면 비교의 긴 한국어 줄바꿈 체감.

## 2026-08-27 — 제2막 감사와 취조실 CSS 부활

- **제2막 6축 감사** (40 에이전트, 각 발견을 반증 검증): 34건 중 **31건 확정 / 3건 기각.**
- **취조실 CSS 98줄이 제2막에서 통째로 죽어 있었다.** 래퍼가 둘인데(`.nb interview` /
  `.act2 interview`) 규칙은 `.nb.interview` 한쪽만 적혀 있었다. 세로 700px 창에서 대화 로그
  높이가 **0** — 며칠간 재현 못 하던 "화면이 짤리고 스크롤이 안 된다"의 정체다.
  `:is(.nb, .act2).interview` 로 수리 (명시도 동일 (0,2,0)).
- **기능 결함 8건 수리** (미커밋 → 이 갱신과 함께 커밋): 심문 중 증거 선택 UI 부재(blocker),
  응답 대기 중 옆방 이동 시 답변·음성 오염(blocker), `animateLast` 가 이름 라벨에 타이핑,
  진술 카드 제시로 대화 1회 증발, 들이밀기 버튼 문구, `connect().message` 유실,
  `restoreFocus` 범위, `.cue-desc` 베이스 부재.
- **인트로 뒤로 넘기기** 구현 — 팀 명세 1-(5) 완료로 1·2·3-1·3-2-(1)(2) 전 항목 소진.
- **Kiro 인수인계 문서** 작성 (`docs/handoff/KIRO-인수인계.md`).
- 팀 명세 `3-2-(5) UI 및 디자인` 수령. **`3-2-(3)`·`3-2-(4)` 는 미수령** — 파일철은 현재
  수사일지를 연다.
- `npm run verify`: 24파일·**324테스트** 통과.

**막힌 것:** 감사 확정 31건 중 23건 미수리. 최우선은 드로어 안 글자 대비 **1.03:1** 이나,
"어두운 판 vs 밝은 종이" 는 디자인 결정이라 3-2-(5) 가이드가 먼저 서야 한다.
디자인 톤 통일 워크플로는 심사 단계에서 중단 — `docs/DESIGN-GUIDE.md` 없음.
`public/characters/*.sit.opt.glb` 2개가 재최적화된 채 미커밋(렌더 미확인, 사람 확인 필요).

## 2026-08-27 밤 — 팀 명세 3-2-(5)·3-3 구현 + GC-001 V0.2 조사 계층 (ADR 031)

**팀 명세 수령 현황이 바뀌었다:** `3-3 취조실` 수령·구현 완료. `GC-001 Hypothesis 중심 구현
명세 V0.2` 수령 — 이건 QA 피드백이 아니라 **핵심 루프 변경의 구현 기준**이다.
`3-2-(3) 수사일지 정보구조`·`3-2-(4) 프로파일링 문서` 는 여전히 **미수령**.

### 배포됨 (번들 해시 대조 완료)
- `index-ArNDqSKI.js` / `index-BZu-OwKm.css` — 3-2-(5) 전체
- `index-ZXfimJPi.js` / `index-BnOVgbhr.css` — 3-3 취조실 + TTS 정책
- `index-DtBDFqY9.js` — V0.2 W1~W3 (조사 계층 엔진)

### 3-2-(5) UI 및 디자인 ✅
`docs/DESIGN-GUIDE.md` + `src/styles/tokens.css` 신설 — 팔레트 다섯 + 놋쇠 한 점, 한 광원,
네 층 질감, **대비는 잰다**(§8). 칠판 전치(세로=사람/가로=시각) + 갓등 원뿔 + 호버 쪽지 +
행 조명. 드로어·사이드바를 같은 종이·먹색으로. 제2막 배경에 벽면 결.

### 3-3 취조실 ✅
상시 노출을 명세 허용 목록으로 줄이고 관계·성향을 수첩 「인물」 면으로 이전. 손에 쥔 기록을
질문 입력 옆으로. 방 재질 채도 -55%(휘도 유지) + 좁은 원뿔. 카메라 1.15m → 0.95m
(**상반신 점유 0.678 → 0.812 · 머리 147 → 178px**). 답변 중 주변 명도 62%.
TTS 는 `voice: 'key'`(중요한 대사만)를 기본값으로 — 품질 판정은 사람 몫이라 되돌릴 수 있는
자리에 두었다.

### GC-001 V0.2 — 「해금」에서 「증명」으로 ✅ (W1~W6·W9)
**기존 엔진 불변.** GC-001 전용 조사 계층을 옆에 세웠다 (ADR 031) — `GameState` 확장은
400시드 결정론과 사전 검증 풀을 깨므로 버렸다.

| 계층 | 파일 | 하는 일 |
|---|---|---|
| Clue·상태 | `engine/inquiry.ts` | Claim/Fact/Evidence 상태기계. 판정 금지·원본 보존·역행 금지 |
| 의도 | `engine/intent.ts` | 결정론 분류 14+1. 화제가 어투를 이긴다 |
| 지식 | `engine/knowledge.ts`, `data/gc001-knowledge.ts` | 말할 수 있는 범위를 코드가 먼저 정한다 |
| 비용 | `engine/askPolicy.ts` | 정상 응답이면 차감, 시스템 실패만 환불 |
| 증명 | `engine/proof.ts`, `data/gc001-proof.ts` | 명제 7개·Closure·Verdict 5종 |
| 화면 | `ui/inquiryPanel.ts`, `ui/proofSheet.ts` | 수사일지 조사 면 · 제출과 판정 |

**실측(실 LLM, 기록 0장):** 류나린 퇴장 주장 → 김하늘에게서 「문 열림 ≠ 통과」 →
그 진술이 `QUESTIONABLE` → 재질문 → `DISPROVED` + 고친 말 `REVISED`(둘 다 남는다) →
문소라에게서 21:18 위치·이동 2분·자격자 둘. **Proof Path B 재료가 조회 0회로 모였다.**
제출 실측: 부실한 논증 → `UNPROVEN`, Path B 근거 6개 → **`PROVEN`**(동기 명제는 미성립인 채로).

테스트 **324 → 519**. AC-01~15 와 §39 금지 6건은 `tests/acceptance.test.ts` 가 조항 번호로 색인.

### 안 되는 것 / 막힌 것 (정직하게)
- **얼굴 텍스처 결함 2종** (housekeeping·expartner): 얼굴 중앙에 세로 이음매. 카메라를 붙이면서
  더 눈에 띈다. 에셋 문제이고 이미지 생성은 하지 않기로 되어 있다 — **사람 확인·교체 필요.**
- `public/characters/*.sit.opt.glb` 두 개가 재최적화된 채 미커밋 (인계 시점 그대로, 렌더 미확인).
- V0.2 **W7 가설 UI 확장**(가설 목록·연결 편집)과 **W8 데이터 정리**(legacy `presentUnlocks`
  사슬을 데이터에서 정식으로 표시)는 남았다. 진행이 막히지는 않는다 — 규칙 엔진의 any-of 와
  Proof 의 두 경로가 이미 그 역할을 한다.
- 3-2-(3)·3-2-(4) 미수령 → 수사일지 **정보구조**는 손대지 않았다 (톤과 대비만 고쳤다).
- 프로덕션에서 V0.2 를 사람이 한 판 끝까지 플레이한 기록은 없다. 헤드리스·브라우저 실측과
  프로덕션 API 스모크는 통과했다.
