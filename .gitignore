node_modules
dist
.DS_Store
.wrangler
.dev.vars
docs/.review-streak

# 원본 3D (크고 번들에 안 들어간다). 최적화본(.opt.glb)만 커밋한다.
public/characters/*.rigged.glb
public/characters/*.glb
!public/characters/*.opt.glb
public/characters/*.seated.glb

# 3D 원본(대용량). 최적화본만 public/ 에 둔다 — Pages 는 25MB 초과 파일을 거부한다.
assets-src/
public/characters/*.mv.glb
public/characters/*.mvrigged.glb
public/characters/*.mvseated.glb
public/characters/*.pbr.glb
assets-src/
