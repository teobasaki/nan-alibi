# 재현 영상이 들어갈 자리

`0.mp4` … `5.mp4` 를 여기 넣으면 검거 엔딩에서 재생된다.
**비어 있으면 아무 일도 일어나지 않는다** — 기존 정지 컷 엔딩이 그대로 나온다
(`src/ui/reenact.ts` 의 `hasReenactment()`).

제작 브리프: [../../../docs/content/재현영상/README.md](../../../docs/content/재현영상/README.md)
샷리스트: `docs/content/재현영상/shotlist.html` (브라우저로 열면 Copy 버튼이 있다)
